# v0.7.9 — the reporter finally reports

**Status: ready when the operator says so.** Patch on v0.7.8, same day. Seven
NOW issues from the full GitHub triage (186 open → 55 after 131 closes) plus
one decision (#40, steer dropped). Test-first throughout.

| Issue | Root cause | Fix |
|---|---|---|
| #2323 | `aos-report` ran `gh` with `cwd=~/aos` (a non-git symlink) and no `--repo`; failures swallowed | `--repo AOS-Agent/aos` on every call; failure exits 1 with a plain-English notice |
| #2338 | `log_report()` had no body parameter — queued reports lost their content | queue keeps body/labels/id; `aos-report --drain` files exactly what was queued |
| #2349 | phone regex matched any 10+ digit/space/dash run | reuses the bridge's single `redact()`; code, tables, dates untouched |
| #114 | `weekly-digest` read `~/vault/ops/sessions` (never existed) | `log/sessions` per SCHEMA.md; zero-session week warns |
| #2298 | the `UTC` constant looked up on the class instead of the module — crashed on every Python | `timezone.utc` in session-recorder, telemetry, check-fixed-issues |
| #2326 | OAuth refresh token in a plaintext file | `gws` owns the file format, so: 0600, Time-Machine-excluded, legacy dir removed (**migration 134**), reconcile NOTIFY on drift; `gmail_ingest.py` repointed |
| #2334 | `generate-connect-script` left an existing `Host aos` block untouched | idempotent marker-delimited block; rest of `~/.ssh/config` byte-identical |
| #40 | `steer` binary never vendored anywhere | `core/steer/` and 24 capability entries removed; the bridge's STEER failure copy no longer promises a retry |

## What to verify after install

- Migration 134 applied; `ls -l ~/.google_workspace_mcp/credentials/` shows 0600; `gws` still authenticates.
- `/report` files an issue end to end (or queues and `aos-report --drain` files it).
- Sunday's weekly digest reports a real session count.

## Rollout

`main` → this Mini on `edge`. `aos promote` after the soak moves `stable` → faisal-mini takes 0.7.7 + 0.7.8 + 0.7.9 together, watched (aos#241).
