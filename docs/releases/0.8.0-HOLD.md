# v0.8.0 — clean and freeze

**Status: HOLD. Do not ship.** Release only when the Qren installer path is
clear. Everything below is built, tested and dry-run; nothing has been applied
to any machine, and the branch has not been pushed or merged.

Branch: `release/0.8.0` · Base: `main` @ `17453bd` · VERSION: `v0.8.0`

---

## What this release does

AOS stops being a system under development. This is the last feature release:
it sheds what was never used, switches off what should not have been on by
default, and changes the update path so this machine keeps taking patches and
nothing else. What comes after is Qren, installed deliberately — not an update
that arrives at 4am.

Three things happen at once, and they are separable if any one of them turns
out to be wrong.

### 1. Four services become opt-in

| Service | Why | Reversal |
|---|---|---|
| `work-runner` | `task_runs` held **zero rows on every machine audited** — 905 lines and a resident KeepAlive'd LaunchAgent that never once delegated a task | `work runner enable` |
| `sentinel` | Reads iMessage, drafts follow-through. Autonomous comms defers to Qren | list under `enabled:`, reload plist |
| `converse` | Live multi-turn sessions. Same | same |
| `envoy` | Runs delegated outbound conversations with third parties. Same | same |

The mechanism is a new `enabled:` list in `~/.aos/config/services.yaml`
alongside the `disabled:` list reconcile already reads. **`enabled:` wins and is
never auto-disabled** — by a migration, by reconcile, by anything. Without that
asymmetry, "off by default" and "off by decree" are the same code path, and an
operator who wants Sentinel gets it switched off under them on every update.

`modules.yaml` had `work-runner` as `tier: core` while the service registry
called it `optional` — the precise drift the registry exists to prevent. Aligned.

### 2. The instance gets cleaned

- **368 test-fixture tasks** deleted from `work.db`. The test suite wrote to the
  real database for ~3.5 months; these are the six literal titles from
  `tests/test_engine.py`, 17% of the task table and the bulk of the `t#` bucket
  in every session-start briefing. Matched on exact title **and** creation
  window **and** zero activity rows, so a real task that happens to share a
  title survives.
- **3,574 auto-generated "Work in …" threads** closed (not deleted). One per
  worktree checkout; 4,059 of 4,061 were still `exploring` and 2 were ever
  promoted. The generation code is deliberately untouched.
- **Three named stale DB backups** removed (~238 MB), each safety-copied first.
- **Qren-readiness inventory** written to `~/.aos/data/qren-readiness.json`.

### 3. Updates freeze

`frozen: true` in `~/.aos/config/update-policy.yaml` means `check-update` offers
patches only. "Patch" is decided from the VERSION numbers — same `major.minor`,
higher `patch` — not from trust in the sender. A frozen machine that cannot read
the candidate's version is offered **nothing**: this gate fails closed, unlike
the services opt-out which fails open, because failing open here pushes an
unidentified release onto a machine that asked to stop receiving them.

One Telegram notice goes out, once, ever (migration 117), in plain English:
AOS is finished, Qren is coming, invite only.

There is no host scope guard. One was built for this release and removed
before it shipped — see **Fleet status** below.

---

## Dry-run numbers

Applied against a full copy of this Mini's `~/.aos` at `/tmp/aos-080-dryrun`,
with `launchctl` and `aos-notify` stubbed. The live instance was verified
untouched afterwards: 2,125 tasks, 4,059 exploring threads, 3 `.bak` files, no
`update-policy.yaml`, no `enabled:` key — all unchanged.

| Measure | Before | After | Delta |
|---|---:|---:|---:|
| tasks | 2,125 | 1,757 | **−368** |
| threads (exploring) | 4,059 | 485 | **−3,574** |
| threads (total) | 4,061 | 4,061 | 0 — closed, never deleted |
| `~/.aos/data` | 519.6 MB | 277.1 MB | **−242.5 MB** |
| `work.db` | 1.88 MB | 1.77 MB | −0.11 MB |

Services that would be recorded off on this machine: `work-runner` (the other
three are already in this Mini's `disabled:` list, so 112 is a no-op here — and
a no-op everywhere, since this is the only machine).

**Net disk is not 242.5 MB.** The purge writes 229 MB of safety copies to
`~/.aos/backups/pre-purge/` first, so immediate reclaim is ~13 MB. The full
242.5 MB lands only when the operator deletes those copies, which is
deliberately their call and not this release's.

All seven migrations were run twice. Every one reported `check() == True` after
both runs, with no further change on the second:

```
111 applied=True idempotent=True      115 applied=True idempotent=True
112 applied=True idempotent=True      116 applied=True idempotent=True
113 applied=True idempotent=True      117 applied=True idempotent=True
114 applied=True idempotent=True
```

Engines detected for the readiness inventory: `claude 2.1.238`,
`codex-cli 0.145.0`, `kimi 0.29.1`. `qren.invite_token` is `""`.

**122 was added after this dry-run and is not in the table above.** It has its
own 7 idempotency tests against a sandbox HOME (`tests/test_migration_122_idempotency.py`),
including a replay after a manual restore and an assertion that the neighbouring
files in `~/.aos/config` come back byte-identical. It moves two files and writes
no database, so there is no DB state for a dry-run to measure — but it has not
been run against a copy of the live `~/.aos`, and running it there is the one
pre-install step this section does not yet cover.

---

## Rollout order

**Only when the Qren installer path is clear.** Then: install it on this Mini.
That is the whole rollout. There is no second machine, no ordering problem, and
nothing to stage — see **Fleet status** below for why.

This Mini (`agents-mac-mini`) is the dev and release machine. It is at migration
level 107, so 108–110 (the decommission sweep, already on `main`) land in the
same update as 111–122.

After `check-update --apply` returns, verify in this order and stop at the first
failure — each one is cheap, and a half-applied freeze release is the thing this
list exists to catch:

1. **`aos self-test`** and **`aos hygiene`** — both clean. These are the two
   commands the ship flow itself now ends on.
2. **Migration level is 122.** `aos migrate --status`. If it stopped short, the
   failing migration's own output says where; `~/.aos/data/update-state.json`
   records it under `last_apply`.
3. **The freeze took.** `~/.aos/config/update-policy.yaml` holds `frozen: true`,
   and `channel-update.yaml` still holds its original hourly-status settings
   (forum_topic_id, include) — 117 must never have written over that file.
4. **`check-update` reports frozen on the next cycle.** It runs every 2h; the
   log line reads `Frozen: …`. A patch must still be offered and a feature
   release must not — that asymmetry is the point of the gate, and
   `tests/test_freeze.py` is what proves the logic, not this step.
5. **The Telegram freeze notice arrived, once.**
   `~/.aos/state/.freeze-notice-sent` exists. If it reads `attempted`, the send
   failed and will deliberately not retry — the CHANGELOG is the durable
   record.
6. **`work-runner` is off and stays off.** It is the only service 111 actually
   changes here. Confirm it is absent from `launchctl list` and that the next
   reconcile does not re-arm it.
7. **The services the operator opted into are still running.** Specifically the
   bridge: 112's reconcile check and the `SentinelPlistDriftCheck` fix are the
   code most likely to switch something off that should be on.

## Fleet status

**Recorded 2026-09-13, by SSH probe, because this release was planned around a
four-machine rollout that does not exist.**

| Node | AOS install | Detail |
|---|---|---|
| `agents-mac-mini` (this Mini) | **yes** | The only one. Dev workspace, runtime, instance data, all services. |
| `pi5` | no | No `~/aos`, no `~/.aos`. Nothing to update. |
| `mbp` | no | `~/.aos/config` only — no framework tree, 0 LaunchAgents. An instance layer with nothing underneath it. |
| `imac` | no | A partial `~/aos/core` with no git history and no `~/.aos` at all (0 agents). A second stray copy under a different local account's home. Not an install; a half-finished copy. |
| `faisal-mini` | out of scope by design | Another account's machine, shared into this tailnet. Never ours to update. |

So the v0.7.x "fleet" was one machine running AOS and three carrying fragments
of it. `aos fleet status` would have reported three unreachable rows and called
it drift.

What this changed in the release, and why none of it is a reversal of the freeze:

- **The host scope guard is removed.** It existed to stop the updater on
  `faisal-mini`. It was never shipped — this branch has never been pushed or
  merged — so `faisal-mini` has never had the guard, and removing it restores
  the status quo that has held all along rather than exposing anything. Nothing
  on this Mini reaches that machine in the first place.
- **`aos fleet` is removed**, with migration 122 retiring
  `~/.aos/config/fleet.yaml` (which listed `local` and `faisal-mini`) to
  `~/.aos/backups/retired-config/`.
- **`catch_up: true` is off the two update crons.** Its recorded reason was the
  laptop that sleeps through 04:00; that laptop has no AOS.
- **The ship flow ends at the local runtime.** Its Step 6 verified remote nodes.

**Do not plan a rollout from the v0.7.x fleet assumptions again.** If a node is
ever added, the thing to write is a new registry and a new verify path, not a
restoration of these — they were built for machines that had already stopped
being installs by the time the release was cut.

## Verification status

- **Test suite: 1,673 passed, 1 skipped** (1,680 before the single-node
  cleanup: −14 host-scope tests, +7 for migration 122). Measured on
  `chore/single-node` off `release/0.8.0` @ `9b5d61b`; that branch has taken
  further commits since (thread-generator fix, memory-curate), so re-run the
  suite after merging rather than trusting this number. One pre-existing
  failure —
  `test_every_service_dir_has_a_manifest` — is a working-tree artifact of the
  decommission sweep (stale `__pycache__` directories under
  `core/services/{companion,listen,memory}`). It fails identically on `main`,
  and a clean clone of this branch passes 13/13.
- **New tests: 87.** 28 for the freeze gate (`tests/test_freeze.py` — was 42
  when it also covered host scope), 19 for the Telegram reply, 33 for migration
  idempotency against a throwaway copy of the real `work.db`, and 7 for
  migration 122.
- **ship-check: 36 passed, 0 warnings, 0 failed** on `chore/single-node`
  (`AOS_SHIP_CHECK_DIR` pointed at the branch checkout — ship-check otherwise
  grades `~/project/aos`, which is a different tree). The earlier record for
  this release read 34 passed / 4 warnings / 1 failed; the `ruff` failure and
  the `setup.sh` warnings were cleared on `release/0.8.0` since. Historical
  note on that failure: 7 findings pre-existed on `main`, 2 were inside
  untracked `core/qareen/screen/node_modules/` left over from the sweep. The
  4 warnings are pre-existing (integrations without `setup.sh`).

## Things found while building this, worth knowing

1. **`SentinelPlistDriftCheck` ignored the operator's opt-out.** It never
   declared `service = "sentinel"`, so reconcile could not know which service it
   owned and called `fix()` — which re-renders the plist and restarts the
   service — for an operator who had switched Sentinel off. Fixed here, along
   with the same gap in `BridgePollLivenessCheck`. This mattered more than it
   looks: it was the exact path that would have re-armed the arm this release
   switches off.
2. **The freeze flag nearly destroyed a working config.** `channel-update.yaml`
   was the obvious filename and has been in use since March holding the hourly
   Telegram status settings. The flag moved to `update-policy.yaml`; the old
   file is now read-only, for an operator who set `frozen:` there by hand.
3. **`default_off.py` resolved `Path.home()` at import time**, so the first
   caller to import it decided, process-wide, whose `services.yaml` everyone
   else wrote to — in a test run, the operator's real machine. Now resolved per
   call, with a test asserting the live instance is untouched after the suite.
4. **The Telegram handler created junk tasks.** Every trigger prefix required a
   trailing space, so a bare `add task:` matched none of them and created a task
   titled "add task:" — the same shape that put `--help` in the task table twice.
5. **The plan's 30-day thread window closes 15 rows of 4,057.** The noise is
   generated continuously and is nearly all recent. Changed to 7 days (3,574
   rows), which still leaves the current working week open.

## Deliberately not done

- **No n8n migration.** The plan called for one, but migration `109` (already on
  `main`, not yet applied here) deletes `~/.aos/services/n8n` entirely. A second
  migration deleting a directory the previous one removed is dead code.
- **The generation of "Work in …" threads is unchanged.** Fixing it is a
  behaviour change, and this is a freeze.
- **`sana-watch`, `ios-deploy`, `qareen-deploy` untouched.** Instance-only on
  this Mini, no plist template, no migration ships them. Marked
  `status_note: instance-only, not installable` in `modules.yaml`; not
  instrumented, per the plan.
- **Nothing applied to any live machine.** No push, no merge, no migration run
  outside `/tmp`.

## Migration index

| # | Name | Mutates | Backup | Reversal |
|---|---|---|---|---|
| 111 | `work_runner_default_off` | services.yaml, plist, work-runner.yaml | n/a | `work runner enable` |
| 112 | `comms_arms_default_off` | services.yaml, boots out agents | n/a | `enabled:` + reload |
| 113 | `purge_stale_db_backups` | deletes 3 named files | timestamped copy | restore copy |
| 114 | `purge_test_fixture_tasks` | deletes `tasks` rows | `work.db` copy | restore copy |
| 115 | `close_stale_explore_threads` | `threads.status` | `work.db` copy | flip status back |
| 116 | `qren_readiness` | writes one JSON report | n/a | delete the file |
| 117 | `freeze` | `update-policy.yaml`, one Telegram notice | n/a | `frozen: false` |
| 120 | `retire_dead_mcp_entries_and_orphans` | archives `services/slack-lite`, `~/.claude.json` mcpServers, `integrations.yaml` status | timestamped copy/backup for each | move dir back / restore `.claude.json.bak-120-*` / hand-edit |
| 122 | `retire_fleet_registry` | moves `config/fleet.yaml` + `config/allow-updates` out | the move itself (archived, not deleted) | `mv` it back from `backups/retired-config/` |

Reconcile gains one check, `default_off_services`, which re-declares the
default-off set if the declaration is lost — and skips any name the operator
opted in to.

### 120's launcher check found nothing to retire

The brief for 120 named four LaunchAgents as stalled and crash-looping:
`com.aos.scheduler`, `com.aos.domain-watch`, `com.aos.domain-catch`,
`am.hish.superwhisper-launcher`. Each was checked against its own log and
`launchctl` state on the reference machine before any code was written, per
the brief's own instruction to verify first. All four were alive and doing
real work — the scheduler was ticking every 5 minutes with 38 jobs defined,
domain-watch and domain-catch were actively tracking `quran.garden` and
`alhuda.com` with clean logs and no errors, and the superwhisper launcher had
correctly no-op'd or launched the app at its last login. None were retired;
migration 120 ships with an explicit empty retirement list for part (a) and
does (b) service-dir orphans, (c) dead MCP entries, and (d) integration status
drift instead. See the migration's own docstring for the full verification
detail.
