"""Ambient Knowledge Phase 5 — injection + knowing\u2192doing.

The digest, per-person snapshots, commitment\u2192inbox proposer, nudge
surfacing, and auth-aware pause that turn the enriched entity store into
context an agent already knows and actions it takes behind the scenes.
"""
